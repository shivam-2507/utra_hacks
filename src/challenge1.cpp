#include <string>
#include <iostream>
#include <cmath>
#include <algorithm>
using namespace std;

void challenge1(){
    // turn 180
    // get init colour
    // append that colour to vector
    
    // move forward
    // if colour changes, append new colour to vector
    // if len(colour_vector) == 6, stop place flag in middle  ** might to be move forwrd before placing**
}